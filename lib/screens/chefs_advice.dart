import 'package:flutter/material.dart';


class ChefsAdvices extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Chefs Advices will be here...'),),
      
    );
  }
}